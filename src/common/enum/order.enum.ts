export enum PaymentStatus {
  UNPAID = 0,
  PAID = 1
}

export enum OrderType {
  RENT_LOCKER = 0,
  SEND_PACKAGE = 1
}

export enum OrderStatus {
  PENDING = 0,
  SENDING = 1,
  RECEIVED = 2,
  EXPIRED = 3
}
